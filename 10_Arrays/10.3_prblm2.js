/* 
    Create a variable greeting and initialise it to :
    " hello, nice to meet you "

    use a loop to loop through this string 
    - on each loop iteration , print out the character
*/

const greeting = " hello nice to meet u "

for( let i = 0 ; i < greeting.length ; i++)
{
    console.log(greeting[i]);
    
}