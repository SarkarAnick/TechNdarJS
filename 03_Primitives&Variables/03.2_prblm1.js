/* 
    1. create a variable named name that points at a string containing ur name
    2. on the next line, re-assign name to your favourite musician's name
    3. on the next line, re-assign the variable again to the value true
    4. one the next line , re-assign the variable one last time to the value: 42

  After each of the above steps, log out of the variable to the erminal with console log
*/

let container = "anick"
console.log(container);

container = "eminem"
console.log(container);

container = true
console.log(container);

container = 42
console.log(container);


